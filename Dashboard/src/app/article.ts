export interface Article {
    body: {
        newId: String,
        oldId: String,
        image: String,
        prefix: String,
        category: String,
        title: String,
        description: String,
        date: String,
        template: String
    }
}