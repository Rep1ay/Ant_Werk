export interface Article {
    body: {
        id: String,
        image: String,
        prefix: String,
        category: String,
        title: String,
        description: String,
        date: String,
        template: String
    }
}