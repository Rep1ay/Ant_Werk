export interface Article {
    body: {
        id: String,
        image: String,
        prefix: String,
        category: String,
        title: String,
        discription: String,
        date: String,
        template: String
    }
}